<ul class="list-group">
  <% if pets.any? %>
    <% pets.each do |pet| %>
      <li class="list-group-item"><%= pet.species_icon %> <%= pet.name %> the <%= pet.species.humanize %></li>
    <% end %>
  <% else %>
    <li class="list-group-item bg-info">No pets to show here</li>
  <% end %>
</ul>